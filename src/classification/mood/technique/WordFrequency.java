package src.classification.mood.technique;

/**
 * This is the interface for WordFrequency, which was part of the original architecture design.
 * However, since each technique now has its own program routine which calls its own methods, this interface really isn't needed.
 * 
 * @author Francis
 */
public interface WordFrequency extends Technique{

}
